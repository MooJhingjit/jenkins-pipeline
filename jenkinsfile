pipeline{
    agent any
    stages{
        stage('Git clone') {
            steps{
                git branch: 'main', url: 'https://github.com/MooJhingjit/jenkins-pipeline.git'
            }
        }
        stage('show all file') {
            steps{
                sh 'ls -la'
            }
        }
    }
}
